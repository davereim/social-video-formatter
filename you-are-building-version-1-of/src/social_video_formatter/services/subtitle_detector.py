from pathlib import Path


class SubtitleDetector:
    def detect(self, file_path: Path) -> str:
        # V1 heuristic placeholder, returns uncertain unless overridden by future vision logic.
        return "uncertain"
